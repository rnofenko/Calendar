@model IEnumerable<Bs.Calendar.Models.User>
@{
    ViewBag.Title = "Home Page";
}

<div class="row">
    
    <h1>Пользователи</h1>

    @foreach (var user in Model)
    {
        <div class="row">
            @user.Email
        </div>
    }
</div>